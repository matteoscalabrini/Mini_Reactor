// HubSleepLogic — implementation is inline in the header (header-only module).
// This translation unit exists solely as a compile-time sanity check that the
// header is well-formed and self-contained on the embedded target.
#include "features/hub/sleep/HubSleepLogic.hpp"
